// Objeto Math

let resultado

resultado = Math.PI;
resultado = Math.round (2.5); //redondea hacia arriba.
resultado = Math.ceil (2.1); //ceil redondea hacia arriba tambien.
resultado = Math.floor (2.9); // floor redondea hacia abajo.
resultado = Math.sqrt (144); // muestra en la consola el resultado de raíz cuadrada.
resultado = Math.abs (-200); // Convierte los números en positivos.
resultado = Math.min (3,5,1,8,2,10); // Nos muestra el número mas pequeño de un listado.
resultado = Math.max (3,5,1,8,2,10);  // Nos muestra el número mas grande de un listado
resultado = Math.floor(Math.random ()* 30); // Nos muestra un número aleatorio. Aquí usamos una función, mas otra función y un operador.
resultado

console.log(resultado);
