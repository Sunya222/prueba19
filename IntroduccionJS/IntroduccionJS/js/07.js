// Orden de las operaciones

let resultado;

resultado = (20 + 30) * 2;

resultado = (600 + 600) * 1.5; // Esto se usa para: Cupones, Impuestos, Boletos de conciertos, etc. Por ejemplo al usar la App te damos un descuento. Es importante el orden de las operaciones. Poner paréntesis al hacerlas, recordemos divisones y multiplicaciones van a tener más peso que sumas y restas.


// console.log(resultado);

// Incrementos // Se usa ésta fórmula por ejemplo cuando se dan likes en la RRSS. Tambien en un video juego cuando un rival pasa de nivel.
let puntaje = 10;

// Incrementos  en 1 ++

puntaje = + 10;

console.log(puntaje);

